{
  "name": "velicot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter studio dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
